<?php
// File ini menangani operasi CRUD untuk entitas promo

// Fungsi untuk membuat promo baru
function createPromo($data) {
    // Logika untuk menambahkan promo ke database
}

// Fungsi untuk membaca data promo
function readPromo($id) {
    // Logika untuk mengambil data promo dari database
}

// Fungsi untuk memperbarui data promo
function updatePromo($id, $data) {
    // Logika untuk memperbarui data promo di database
}

// Fungsi untuk menghapus promo
function deletePromo($id) {
    // Logika untuk menghapus promo dari database
}
